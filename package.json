{
  "name": "root",
  "private": true,
  "scripts": {
    "deploy": "lerna run ui:build --stream && lerna run ui:publish --stream",
    "host": "lerna run service:host"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
